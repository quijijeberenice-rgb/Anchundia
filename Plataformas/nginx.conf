worker_processes auto;
error_log /dev/stderr;
pid /tmp/nginx.pid;

events {}

http {
    server {
        listen 8080;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }
    }
}